description:
  original: replicas is the number of Pods created by the StatefulSet controller.
  formatted: replicas is the number of Pods created by the StatefulSet controller.
type: integer
required: true
