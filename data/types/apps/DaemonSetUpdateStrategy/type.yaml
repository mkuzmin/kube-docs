description:
  original: Type of daemon set update. Can be "RollingUpdate" or "OnDelete". Default
    is RollingUpdate.
  formatted: Type of daemon set update. Can be "RollingUpdate" or "OnDelete". Default
    is RollingUpdate.