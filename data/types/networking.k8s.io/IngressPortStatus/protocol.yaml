description:
  original: 'protocol is the protocol of the ingress port. The supported values are:
    "TCP", "UDP", "SCTP"'
  formatted: 'protocol is the protocol of the ingress port. The supported values are:
    "TCP", "UDP", "SCTP"'
type: string
required: true
