description:
  original: backend defines the referenced service endpoint to which the traffic will
    be forwarded to.
  formatted: backend defines the referenced service endpoint to which the traffic
    will be forwarded to.