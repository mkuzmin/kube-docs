description:
  original: readOnly here will force the ReadOnly setting in VolumeMounts. Defaults to false.
  formatted: readOnly here will force the ReadOnly setting in VolumeMounts. Defaults to false.
type: boolean
