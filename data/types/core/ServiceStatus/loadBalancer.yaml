description:
  original: LoadBalancer contains the current status of the load-balancer, if one
    is present.
  formatted: LoadBalancer contains the current status of the load-balancer, if one
    is present.