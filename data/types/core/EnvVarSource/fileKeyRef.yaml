description:
  original: FileKeyRef selects a key of the env file. Requires the EnvFiles feature
    gate to be enabled.
  formatted: FileKeyRef selects a key of the env file. Requires the EnvFiles feature
    gate to be enabled.