description:
  original: The IP protocol for this port. Must be UDP, TCP, or SCTP. Default is TCP.
  formatted: The IP protocol for this port. Must be UDP, TCP, or SCTP. Default is
    TCP.
type: string
