description:
  original: |-
    volumePath is the path that identifies vSphere volume vmdk
  formatted: |-
    volumePath is the path that identifies vSphere volume vmdk
type: string
required:
  openapi: true
