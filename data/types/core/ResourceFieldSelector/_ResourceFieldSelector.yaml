description:
  original: ResourceFieldSelector represents container resources (cpu, memory) and
    their output format
  formatted: ResourceFieldSelector represents container resources (cpu, memory) and
    their output format