description:
  original: |-
    `name` is the name of the priority level configuration being referenced Required.
  formatted: |-
    `name` is the name of the priority level configuration being referenced Required.
type: string
required:
  openapi: true
