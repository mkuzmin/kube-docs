description:
  original: |-
    ValidatingWebhookConfiguration describes the configuration of and admission webhook that accept or reject and object without changing it.
  formatted: |-
    Describes the configuration of an admission webhook that accept or reject an object without changing it.
kind: true
