description:
  original: |-
    APIGroups is the API groups the resources belong to. '*' is all groups. If '*' is present, the length of the slice must be one. Required.
  formatted: |-
    The API groups the resources belong to. `*` is all groups. If `*` is present, the length of the slice must be one.
type: string
collection: array
required: true
