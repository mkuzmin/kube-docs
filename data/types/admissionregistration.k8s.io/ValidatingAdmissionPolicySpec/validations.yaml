description:
  original: |-
    Validations contain CEL expressions which is used to apply the validation. Validations and AuditAnnotations may not both be empty; a minimum of one Validations or AuditAnnotations is required.
  formatted: |-
    CEL expressions which is used to apply the validation. `validations` and `auditAnnotations` may not both be empty; a minimum of one `validations` or `auditAnnotations` is required.
type: Validation
collection: array
