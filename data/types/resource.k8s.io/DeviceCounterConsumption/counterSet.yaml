description:
  original: |-
    CounterSet is the name of the set from which the counters defined will be consumed.
  formatted: |-
    CounterSet is the name of the set from which the counters defined will be consumed.
type: string
required:
  openapi: true
