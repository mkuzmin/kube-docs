description:
  original: fieldSelector describes the limitation on access based on field.  It can only limit access, not broaden it.
  formatted: fieldSelector describes the limitation on access based on field.  It can only limit access, not broaden it.
type: FieldSelectorAttributes
