description:
  original: Status is filled in by the server and indicates the set of actions a user
    can perform.
  formatted: Status is filled in by the server and indicates the set of actions a
    user can perform.